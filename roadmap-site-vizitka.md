# 🤖 Роадмап для Claude Code: Сайт-визитка разработчика

> **Для кого:** Claude Code - AI ассистент для разработки  
> **Цель:** Создать профессиональный сайт-визитку с крутыми анимациями, градиентами и современным дизайном  
> **Стек:** HTML + CSS + Vanilla JS (минимум библиотек)

---

## 📋 Оглавление
1. [Общая концепция](#общая-концепция)
2. [Структура проекта](#структура-проекта)
3. [Секции сайта с описанием](#секции-сайта-с-описанием)
4. [Дизайн система](#дизайн-система)
5. [Анимации и эффекты](#анимации-и-эффекты)
6. [Адаптивность](#адаптивность)
7. [Интерактивность](#интерактивность)
8. [Финальные штрихи](#финальные-штрихи)

---

## 🎯 Общая концепция

### Стиль дизайна
**Современный минимализм с акцентом на градиенты и плавные анимации**

**Ключевые характеристики:**
- Чистый white space
- Яркие градиентные акценты
- Плавные переходы между секциями
- Микроанимации при взаимодействии
- Glassmorphism эффекты (опционально)
- Particle effects или animated background (опционально)

### Цветовая схема
**Главные цвета:**
- Первичный градиент: синий → фиолетовый (например, #3B82F6 → #8B5CF6)
- Или: оранжевый → розовый (#F59E0B → #EC4899)
- Или: зелёный → голубой (#10B981 → #06B6D4)

**Выбери один из этих вариантов или предложи свой!**

### Технологии
- **HTML5** - семантическая разметка
- **CSS3** - кастомные свойства, Grid, Flexbox, анимации
- **Vanilla JavaScript** - интерактивность без фреймворков
- **AOS library** - для scroll анимаций (легкая библиотека)
- **Font Awesome** - иконки

---

## 📁 Структура проекта

```
portfolio/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
├── assets/
│   ├── images/
│   │   ├── profile.jpg
│   │   └── projects/
│   │       ├── yorishop.png
│   │       ├── nextbank.png
│   │       ├── carsharex.png
│   │       └── woodendot.png
│   └── resume/
│       └── Резюме_Пашечко_Владислав_Fullstack.pdf
└── README.md
```

**Задача для Claude Code:**
Создай эту структуру папок. Все файлы будут заполняться по мере выполнения роадмапа.

---

## 🎨 Секции сайта с описанием

### 1. Hero Section (Главный экран)

**Задача:**
Создай впечатляющий первый экран с анимированным фоном.

**Что должно быть:**
- **Левая сторона:**
  - Имя крупным шрифтом (плавное появление снизу вверх)
  - Должность с градиентным текстом
  - Короткое описание (2-3 предложения)
  - Две кнопки: "Связаться" и "Скачать резюме"
  - Иконки соцсетей (GitHub, Telegram, Email) с hover эффектами

- **Правая сторона:**
  - Фото или аватар в круглой рамке
  - Градиентный border вокруг фото с анимацией вращения
  - Или: абстрактная геометрия вместо фото

**Фон:**
- Тонкий градиентный фон
- Animated gradient (плавно меняющийся)
- Или: particles эффект (летающие точки)
- Или: animated mesh gradient

**Высота:**
Полный экран (100vh), контент по центру

**Ключевые анимации:**
- Текст появляется с fade-in + translateY
- Кнопки с scale эффектом при hover
- Соцсети с rotate при hover
- Фото с subtle float анимацией

---

### 2. About Section (О себе)

**Задача:**
Рассказать о себе с карточками навыков.

**Что должно быть:**
- Заголовок секции
- 2-3 параграфа текста о тебе
- Сетка из 3-4 карточек с иконками:
  - Frontend (иконка кода)
  - Backend (иконка сервера)
  - Tools (иконка инструментов)
  - Soft Skills (иконка людей)

**Эффекты для карточек:**
- Glassmorphism стиль (полупрозрачный фон с blur)
- Hover эффект: поднятие карточки (translateY) + увеличение тени
- Gradient border при hover
- Subtle rotate при hover (2-3 градуса)

**Анимация появления:**
- Карточки появляются по очереди при скролле (stagger эффект)
- Каждая карточка с задержкой 100-150ms

---

### 3. Skills Section (Технические навыки)

**Задача:**
Показать технологии красиво и современно.

**Вариант 1 - Категории с тегами:**
- Три блока: Frontend, Backend, Tools
- Внутри каждого - теги технологий
- Теги с градиентным фоном или border
- Hover эффект: scale + изменение цвета

**Вариант 2 - Floating cards:**
- Карточки технологий в хаотичном порядке
- Каждая карточка с легкой анимацией float
- При hover - увеличение и поворот

**Эффекты:**
- Теги появляются с fade + scale при скролле
- Ripple эффект при клике на тег
- Gradient background для всей секции

**Альтернативный фон:**
- Светлый с тонкой сеткой (grid pattern)
- Или: контрастный тёмный блок

---

### 4. Projects Section (Проекты)

**Задача:**
Показать 4 лучших проекта максимально эффектно.

**Структура:**
Grid 2x2 на десктопе, колонка на мобилке.

**Каждая карточка проекта:**
- Скриншот проекта (большое изображение)
- Overlay с градиентом при hover
- На overlay появляются:
  - Название проекта
  - Краткое описание
  - Технологии (теги)
  - Две кнопки: "Live Demo" и "GitHub"

**Hover эффект на карточке:**
- Image zoom (scale 1.05-1.1)
- Overlay появляется с fade
- Кнопки slide up
- Легкий 3D tilt эффект (perspective)

**Анимации:**
- Карточки появляются при скролле
- Stagger эффект (по диагонали или волной)
- Плавные transitions (0.3-0.5s)

**Дополнительно:**
- Gradient border вокруг карточек
- Box shadow с цветным glow

---

### 5. Contact Section (Контакты)

**Задача:**
Простая и удобная секция для связи.

**Что должно быть:**
- Заголовок с градиентом
- Подзаголовок "Открыт к предложениям"
- 4 способа связи в виде красивых кнопок/карточек:
  - Email (с иконкой)
  - Telegram (с иконкой)
  - GitHub (с иконкой)
  - Телефон (с иконкой)

**Эффекты:**
- Кнопки контактов с gradient background
- Hover: увеличение + glow эффект
- Ripple animation при клике
- Иконки с rotation при hover

**Фон:**
- Тонкий gradient
- Или: particles в фоне
- Или: animated mesh

---

### 6. Footer

**Задача:**
Минималистичный футер.

**Что должно быть:**
- Copyright
- Ссылка "Наверх" с плавным скроллом
- Иконки соцсетей (повтор из Hero)

**Стиль:**
- Тёмный фон или gradient
- Контрастный текст
- Маленький padding

---

## 🎨 Дизайн система

### CSS Переменные

**Создай систему CSS переменных для:**

**Цвета:**
- Primary gradient (2-3 цвета)
- Secondary gradient
- Dark color (для текста)
- Light color (для фона)
- White
- Accent colors (опционально)

**Spacing:**
- Базовый unit (например, 8px)
- Spacing scale: xs, sm, md, lg, xl, 2xl

**Тени:**
- Shadow small
- Shadow medium
- Shadow large
- Colored shadows (с оттенком primary цвета)

**Borders:**
- Border radius (8px, 12px, 16px, 24px, 50%)

**Typography:**
- Font family (Inter или Poppins)
- Font sizes (responsive с clamp)
- Font weights
- Line heights

**Transitions:**
- Duration (0.2s, 0.3s, 0.5s)
- Timing functions

---

### Типографика

**Требования:**

**Заголовки:**
- H1: Очень крупный (clamp от 2.5rem до 4rem)
- H2: Крупный (clamp от 2rem до 3rem)
- H3: Средний (clamp от 1.5rem до 2rem)
- Все заголовки: font-weight 700, tight line-height

**Параграфы:**
- Размер: clamp от 1rem до 1.125rem
- Line-height: 1.7-1.8
- Color: немного светлее чёрного для читаемости

**Градиентный текст:**
Для заголовков применяй gradient text эффект:
- Background-clip: text
- Gradient background
- Transparent color

---

### Градиенты

**Создай несколько типов градиентов:**

**1. Основной градиент (для фона):**
- Linear gradient под углом 135deg
- От primary к secondary цвету
- Тонкий и subtle

**2. Яркий градиент (для акцентов):**
- Linear gradient
- Более контрастный
- Для кнопок и hover эффектов

**3. Радиальный градиент:**
- Для круглых элементов (фото, кнопки)
- От центра к краям

**4. Mesh gradient:**
- Сложный градиент с несколькими цветами
- Для фонов секций

**5. Animated gradient:**
- Градиент, который плавно меняется
- Используй CSS animation + background-position
- Или: multiple backgrounds с разными animations

---

## ✨ Анимации и эффекты

### Базовые анимации при загрузке

**Hero section:**
- Заголовок: fade in + slide up (0.8s)
- Подзаголовок: fade in + slide up (1s, delay 0.2s)
- Описание: fade in + slide up (1.2s, delay 0.4s)
- Кнопки: fade in + scale (1s, delay 0.6s)
- Фото: fade in + scale (1.2s, delay 0.3s)

**Используй:**
- CSS animations или transitions
- Stagger эффект (каждый элемент с задержкой)

---

### Анимации при скролле (AOS)

**Используй библиотеку AOS:**

**Типы анимаций:**
- fade-up - для текста и карточек
- fade-left/right - для элементов слева/справа
- zoom-in - для проектов
- flip - для особых элементов

**Настройки:**
- Duration: 800-1000ms
- Once: true (анимация только один раз)
- Offset: 100-200px (когда начинать)
- Delay: stagger для группы элементов

**Где применять:**
- About section - карточки fade-up с stagger
- Skills section - теги zoom-in по очереди
- Projects - карточки fade-up + scale
- Contact - кнопки fade-up

---

### Hover эффекты

**Кнопки:**
- Scale (1 → 1.05)
- Box shadow увеличивается
- Gradient shift (изменение позиции)
- Transition 0.3s ease

**Карточки:**
- translateY (-5px до -10px)
- Box shadow увеличивается
- Border glow (gradient border)
- Subtle rotate (2deg)

**Иконки соцсетей:**
- Rotate (360deg)
- Scale (1.2)
- Color change
- Transition 0.3s

**Теги технологий:**
- Scale (1.1)
- Background brightness увеличивается
- Box shadow появляется

**Ссылки:**
- Underline появляется с анимацией
- Или: gradient underline
- Color transition

---

### Продвинутые эффекты

**1. Glassmorphism (опционально):**
- Backdrop-filter: blur(10px)
- Background: rgba с прозрачностью
- Border: 1px solid rgba(белый, 0.2)
- Box shadow

**2. Particle background (опционально):**
- Canvas с JS
- Летающие точки
- Соединяются линиями при приближении
- Реагируют на курсор

**3. Animated gradient background:**
- Background с несколькими слоями
- Каждый слой движется с разной скоростью
- Создаёт эффект глубины

**4. Cursor trail effect (опционально):**
- Градиентный trail за курсором
- Исчезает плавно

**5. Magnetic buttons:**
- Кнопки притягиваются к курсору при приближении
- Subtle эффект

**6. Parallax scrolling:**
- Фоновые элементы двигаются медленнее
- Создаёт depth

**7. 3D tilt cards:**
- Карточки наклоняются при наведении
- Следуют за курсором
- Используй transform: perspective + rotateX/Y

---

## 📱 Адаптивность

### Breakpoints

**Создай responsive для:**
- Mobile: < 640px
- Tablet: 640px - 1023px
- Desktop: >= 1024px
- Large Desktop: >= 1440px

### Mobile-first подход

Начни стили с мобильной версии, затем добавляй media queries для больших экранов.

### Что меняется на мобилке:

**Hero:**
- Стэк вертикально (фото сверху или снизу)
- Меньшие размеры шрифтов
- Кнопки на всю ширину или stack вертикально

**Navigation:**
- Burger menu вместо горизонтального меню
- Fullscreen menu с анимацией

**Projects:**
- Grid 1 колонка вместо 2x2
- Полноширинные карточки

**Skills:**
- Теги на всю ширину или меньше колонок

**Spacing:**
- Меньше padding у секций
- Меньше margins

### Grid и Flexbox

**Grid для:**
- Projects layout
- Skills categories
- Auto-fit для адаптивности

**Flexbox для:**
- Navigation
- Hero layout
- Contact buttons
- Footer

---

## 🎮 Интерактивность (JavaScript)

### 1. Smooth Scroll Navigation

**Задача:**
Плавный скролл к секциям при клике на nav links.

**Как:**
- Слушай клики на nav links
- preventDefault
- Используй scrollIntoView с smooth поведением
- Или: плавная анимация скролла с easing

---

### 2. Активная навигация

**Задача:**
Подсвечивать активную секцию в меню при скролле.

**Как:**
- Слушай scroll event
- Определяй, какая секция сейчас в viewport
- Добавляй класс active к соответствующему nav link
- Убирай active у остальных

**Эффект:**
- Active link с gradient background
- Или: underline
- Smooth transition

---

### 3. Burger Menu

**Задача:**
Мобильное меню с анимацией.

**Как:**
- Кнопка burger (3 линии)
- При клике - toggle класс active
- Меню появляется с анимацией (slide/fade)
- Burger превращается в X

**Анимация burger:**
- Верхняя линия rotate + translate
- Средняя исчезает (opacity 0)
- Нижняя rotate + translate
- Результат: крестик

**Анимация меню:**
- Fullscreen overlay
- Fade in + scale или slide from right
- Links появляются stagger

---

### 4. Cursor Effects (опционально)

**Задача:**
Кастомный курсор с trail эффектом.

**Как:**
- Скрой стандартный cursor
- Создай div который следует за мышью
- Gradient круг с blur
- Smooth following с небольшой задержкой

---

### 5. Тема (опционально)

**Задача:**
Переключатель светлой/тёмной темы.

**Как:**
- Toggle кнопка (солнце/луна)
- При клике - переключай CSS переменные
- Или: добавляй класс dark к body
- Сохраняй выбор в localStorage

**Анимация:**
- Плавная transition всех цветов
- Rotation иконки

---

### 6. Lazy Loading Images

**Задача:**
Загружай изображения проектов только когда они близко к viewport.

**Как:**
- Используй loading="lazy" в img
- Или: Intersection Observer API
- Placeholder blur пока грузится

---

### 7. Copy Email

**Задача:**
Кнопка копирования email в буфер.

**Как:**
- При клике на email - копируй в clipboard
- Покажи tooltip "Скопировано!"
- Tooltip исчезает через 2s

---

## 🎯 Финальные штрихи

### Микроинтерактивности

**Добавь маленькие детали:**

**1. Ripple effect на кнопках:**
- При клике - круг расширяется из точки клика
- Исчезает плавно

**2. Shake animation на hover:**
- Для иконок соцсетей
- Subtle shake (2-3 градуса)

**3. Number counter:**
- Если добавишь статистику (лет опыта, проектов)
- Цифры считают при появлении в viewport

**4. Progress indicator:**
- Тонкая линия сверху показывает прогресс скролла
- Gradient line

**5. Scroll to top button:**
- Появляется после скролла вниз
- Fade in + scale
- Fixed position справа внизу

---

### Performance Optimization

**1. Изображения:**
- Оптимизируй размер (< 200KB каждая)
- Используй WebP с fallback к JPG
- Lazy loading
- Правильные размеры (не грузи 4K для 400px)

**2. CSS:**
- Минифицируй для продакшна
- Убери неиспользуемые стили
- Critical CSS inline в head

**3. JavaScript:**
- Минифицируй
- Defer или async для скриптов
- Убери console.logs

**4. Fonts:**
- Preload основной шрифт
- Font-display: swap
- Используй только нужные weights

**5. Animations:**
- Используй transform и opacity (GPU accelerated)
- Избегай animate width/height/top/left
- will-change для анимируемых элементов

---

### SEO и Accessibility

**Meta tags:**
- Title (< 60 символов)
- Description (< 160 символов)
- Keywords
- Open Graph для соцсетей
- Twitter Card

**Semantic HTML:**
- header, nav, main, section, article, footer
- Правильная иерархия заголовков (h1 → h2 → h3)

**Accessibility:**
- Alt text у всех изображений
- Aria-labels для иконочных кнопок
- Keyboard navigation работает
- Focus states видны
- Контраст цветов (минимум 4.5:1)

**Performance:**
- Lighthouse score > 90
- First Contentful Paint < 2s
- Largest Contentful Paint < 2.5s

---

## 📦 Деплой

### Варианты хостинга:

**1. GitHub Pages (рекомендую):**
- Бесплатно
- Автоматический деплой
- username.github.io

**2. Netlify:**
- Бесплатно
- Auto deploy при push
- Кастомный домен
- HTTPS автоматом

**3. Vercel:**
- Аналогично Netlify
- Очень быстрый

---

## ✅ Чеклист готовности

### Функциональность
- [ ] Все ссылки работают
- [ ] Резюме скачивается
- [ ] Навигация smooth scrolling
- [ ] Burger menu работает на мобилке
- [ ] Все секции видны

### Дизайн
- [ ] Градиенты красивые и уместные
- [ ] Анимации плавные (не дёргаются)
- [ ] Hover эффекты работают
- [ ] AOS анимации срабатывают
- [ ] Цветовая схема гармоничная
- [ ] Типографика читаемая

### Адаптивность
- [ ] Выглядит хорошо на 320px
- [ ] Выглядит хорошо на 768px
- [ ] Выглядит хорошо на 1920px
- [ ] Нет горизонтального скролла
- [ ] Изображения не растянуты

### Performance
- [ ] Изображения оптимизированы
- [ ] Загрузка < 3 секунд
- [ ] Lighthouse score > 85
- [ ] Нет console errors
- [ ] Анимации не лагают

### SEO
- [ ] Meta tags заполнены
- [ ] Semantic HTML используется
- [ ] Alt текст у изображений
- [ ] Heading hierarchy правильная
- [ ] Favicon добавлен

---

## 💡 Советы для Claude Code

### Приоритеты при создании:

**1. Сначала структура:**
- HTML с правильной семантикой
- Все секции по порядку
- Placeholder контент

**2. Потом базовые стили:**
- CSS переменные
- Typography
- Layout (Grid/Flexbox)
- Colors

**3. Затем визуал:**
- Градиенты
- Shadows
- Borders
- Spacing

**4. После - анимации:**
- Basic transitions
- AOS setup
- Hover effects
- Keyframe animations

**5. В конце - JS:**
- Navigation
- Burger menu
- Smooth scroll
- Extras

### Что важно помнить:

**Performance > Красота:**
- Не перегружай анимациями
- Оптимизируй изображения
- Минимизируй JS

**Mobile-first:**
- Начни с мобильной версии
- Адаптивность важнее всего

**Простота > Сложность:**
- Чистый код лучше навороченного
- Меньше JS - лучше
- Vanilla > библиотеки (где возможно)

**Consistency:**
- Единый spacing
- Единый timing для анимаций
- Единая color palette

---

## 🎨 Вдохновение для дизайна

**Где смотреть примеры:**
- Dribbble - portfolio
- Awwwards - developer portfolios
- Behance - web design
- CodePen - animations

**Что искать:**
- Gradient effects
- Modern portfolio
- Developer portfolio
- Minimalist design
- Animated portfolio

---

## 🚀 Итоговый результат

**Что должно получиться:**

**Визуально:**
- Современный, чистый дизайн
- Яркие градиенты в меру
- Плавные анимации
- Профессиональный вид

**Технически:**
- Быстрая загрузка
- Адаптивный на всех устройствах
- Работает везде (кросс-браузерно)
- Оптимизирован для SEO

**Функционально:**
- Легко найти контакты
- Просто скачать резюме
- Удобная навигация
- Приятно взаимодействовать

---

## 🎯 Финальная инструкция для Claude Code

**Создай сайт-визитку, следуя этому плану:**

1. **Начни с HTML структуры** - все секции, семантика
2. **Настрой CSS систему** - переменные, типографика, layout
3. **Добавь градиенты и цвета** - сделай визуально привлекательно
4. **Реализуй адаптивность** - mobile-first, все breakpoints
5. **Добавь базовые анимации** - transitions, hover effects
6. **Подключи AOS** - scroll animations для секций
7. **Напиши JavaScript** - navigation, burger menu, smooth scroll
8. **Добавь микроинтерактивности** - ripple, parallax, extras
9. **Оптимизируй** - изображения, код, performance
10. **Протестируй** - на всех устройствах и браузерах

**Помни:**
- Красота в простоте
- Анимации должны быть плавными, не раздражающими
- Градиенты - акценты, не основной фон
- Mobile UX = Desktop UX
- Быстрая загрузка важнее всего

**Удачи! 🚀**

---

## 📱 Секции сайта

### 1. **Hero Section (Главный экран)**
**Что включить:**
- Твоё имя и должность
- Короткий слоган или описание (1-2 предложения)
- Фото или аватар
- CTA кнопки: "Скачать резюме", "Связаться со мной"
- Ссылки на соцсети (GitHub, Telegram, Email)

**Пример структуры:**
```html
<section id="hero">
  <div class="container">
    <div class="hero-content">
      <h1 class="hero-title">Владислав Пашечко</h1>
      <p class="hero-subtitle">Fullstack разработчик</p>
      <p class="hero-description">
        Создаю современные веб-приложения с использованием Vue, React и Python
      </p>
      <div class="hero-buttons">
        <a href="#contact" class="btn btn-primary">Связаться</a>
        <a href="assets/resume/resume.pdf" class="btn btn-secondary" download>
          Скачать резюме
        </a>
      </div>
      <div class="hero-socials">
        <a href="https://github.com/peklos"><i class="fab fa-github"></i></a>
        <a href="https://t.me/swslt1616"><i class="fab fa-telegram"></i></a>
        <a href="mailto:vladpashechko@mail.ru"><i class="fas fa-envelope"></i></a>
      </div>
    </div>
  </div>
</section>
```

---

### 2. **About Section (О себе)**
**Что включить:**
- Краткая биография (2-3 абзаца)
- Ключевые навыки в виде карточек или иконок
- Твой подход к работе

**Пример структуры:**
```html
<section id="about">
  <div class="container">
    <h2 class="section-title">О себе</h2>
    <div class="about-content">
      <div class="about-text">
        <p>
          Fullstack-разработчик с опытом создания веб-приложений и 
          Telegram-ботов. Занимаюсь веб-разработкой с сентября 2023 года...
        </p>
      </div>
      <div class="skills-grid">
        <div class="skill-card" data-aos="fade-up">
          <i class="fab fa-vuejs"></i>
          <h3>Frontend</h3>
          <p>Vue, React, TypeScript</p>
        </div>
        <div class="skill-card" data-aos="fade-up" data-aos-delay="100">
          <i class="fab fa-python"></i>
          <h3>Backend</h3>
          <p>Python, FastAPI, PostgreSQL</p>
        </div>
        <!-- Добавь больше карточек -->
      </div>
    </div>
  </div>
</section>
```

---

### 3. **Skills Section (Навыки)**
**Варианты представления:**
- Карточки с иконками
- Прогресс-бары (НЕ рекомендую - выглядит устарело)
- Облако тегов
- Простой список с иконками

**Рекомендую: Карточки или теги**
```html
<section id="skills">
  <div class="container">
    <h2 class="section-title">Технические навыки</h2>
    
    <div class="skills-category">
      <h3>Frontend</h3>
      <div class="tech-tags">
        <span class="tag">HTML</span>
        <span class="tag">CSS</span>
        <span class="tag">JavaScript</span>
        <span class="tag">TypeScript</span>
        <span class="tag">Vue.js</span>
        <span class="tag">React</span>
        <span class="tag">Tailwind CSS</span>
      </div>
    </div>

    <div class="skills-category">
      <h3>Backend</h3>
      <div class="tech-tags">
        <span class="tag">Python</span>
        <span class="tag">FastAPI</span>
        <span class="tag">PostgreSQL</span>
        <span class="tag">MySQL</span>
      </div>
    </div>
  </div>
</section>
```

---

### 4. **Projects Section (Проекты)**
**Самая важная секция!**

**Что показать:**
- 3-4 лучших проекта (не больше!)
- Скриншоты или превью
- Краткое описание
- Используемый стек
- Ссылки на GitHub и Live Demo

**Пример структуры:**
```html
<section id="projects">
  <div class="container">
    <h2 class="section-title">Ключевые проекты</h2>
    
    <div class="projects-grid">
      <!-- Проект 1 -->
      <div class="project-card" data-aos="fade-up">
        <div class="project-image">
          <img src="assets/images/projects/yorishop.png" alt="YoriShop">
          <div class="project-overlay">
            <a href="https://peklos.github.io/YoriShop" class="btn-link">Live Demo</a>
            <a href="https://github.com/peklos/YoriShop" class="btn-link">GitHub</a>
          </div>
        </div>
        <div class="project-info">
          <h3>YoriShop</h3>
          <p>MVP интернет-магазина одежды на Vue.js</p>
          <div class="project-tech">
            <span class="tag">Vue.js</span>
            <span class="tag">Pinia</span>
            <span class="tag">Tailwind</span>
          </div>
        </div>
      </div>

      <!-- Повтори для остальных проектов -->
    </div>
  </div>
</section>
```

---

### 5. **Contact Section (Контакты)**
**Что включить:**
- Email
- Telegram
- GitHub
- Форма обратной связи (опционально, можно через mailto)

**Простой вариант:**
```html
<section id="contact">
  <div class="container">
    <h2 class="section-title">Связаться со мной</h2>
    <div class="contact-content">
      <p class="contact-description">
        Открыт к предложениям о работе и интересным проектам
      </p>
      
      <div class="contact-methods">
        <a href="mailto:vladpashechko@mail.ru" class="contact-item">
          <i class="fas fa-envelope"></i>
          <span>vladpashechko@mail.ru</span>
        </a>
        <a href="https://t.me/swslt1616" class="contact-item">
          <i class="fab fa-telegram"></i>
          <span>@swslt1616</span>
        </a>
        <a href="https://github.com/peklos" class="contact-item">
          <i class="fab fa-github"></i>
          <span>github.com/peklos</span>
        </a>
        <a href="tel:+79003703668" class="contact-item">
          <i class="fas fa-phone"></i>
          <span>+7 900 370 36 68</span>
        </a>
      </div>
    </div>
  </div>
</section>
```

---

### 6. **Footer**
```html
<footer>
  <div class="container">
    <p>&copy; 2024 Владислав Пашечко. Все права защищены.</p>
    <div class="footer-links">
      <a href="#hero">Наверх</a>
    </div>
  </div>
</footer>
```

---

## 🎨 Лучшие практики дизайна

### Цветовая палитра
**Рекомендую минималистичную схему:**

```css
:root {
  /* Основные цвета */
  --primary: #3B82F6;      /* Синий - акцент */
  --secondary: #8B5CF6;     /* Фиолетовый - вторичный */
  --dark: #1F2937;          /* Тёмно-серый - текст */
  --light: #F9FAFB;         /* Светлый - фон */
  --white: #FFFFFF;
  
  /* Градиенты */
  --gradient: linear-gradient(135deg, var(--primary), var(--secondary));
  
  /* Тени */
  --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
  --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
  --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
}
```

**Альтернативные схемы:**
- **Тёмная тема**: Чёрный фон + яркие акценты
- **Минимализм**: Белый + один яркий цвет
- **Градиенты**: Используй subtle градиенты

---

### Типографика

```css
/* Базовая типографика */
body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 16px;
  line-height: 1.6;
  color: var(--dark);
}

h1 {
  font-size: clamp(2rem, 5vw, 3.5rem);
  font-weight: 700;
  line-height: 1.2;
}

h2 {
  font-size: clamp(1.75rem, 4vw, 2.5rem);
  font-weight: 600;
  line-height: 1.3;
}

h3 {
  font-size: clamp(1.25rem, 3vw, 1.75rem);
  font-weight: 600;
}

p {
  font-size: clamp(1rem, 2vw, 1.125rem);
  line-height: 1.7;
}
```

**Важно:**
- Используй `clamp()` для адаптивных размеров
- Соблюдай иерархию заголовков
- Максимум 2-3 шрифта на сайте

---

### Spacing и Layout

```css
/* Контейнер */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 20px;
}

/* Секции */
section {
  padding: 80px 0;
}

@media (max-width: 768px) {
  section {
    padding: 60px 0;
  }
}

/* Spacing система (8px base) */
.spacing-xs { margin: 8px; }
.spacing-sm { margin: 16px; }
.spacing-md { margin: 24px; }
.spacing-lg { margin: 32px; }
.spacing-xl { margin: 48px; }
```

---

### Компоненты

#### Кнопки
```css
.btn {
  display: inline-block;
  padding: 12px 32px;
  border-radius: 8px;
  font-weight: 500;
  text-decoration: none;
  transition: all 0.3s ease;
  cursor: pointer;
}

.btn-primary {
  background: var(--primary);
  color: var(--white);
}

.btn-primary:hover {
  background: #2563EB;
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

.btn-secondary {
  background: transparent;
  color: var(--primary);
  border: 2px solid var(--primary);
}

.btn-secondary:hover {
  background: var(--primary);
  color: var(--white);
}
```

#### Карточки
```css
.card {
  background: var(--white);
  border-radius: 12px;
  padding: 24px;
  box-shadow: var(--shadow-md);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-lg);
}
```

---

## 📱 Адаптивность и отзывчивость

### Breakpoints
```css
/* Mobile First подход */

/* Small devices (phones, < 640px) */
@media (max-width: 639px) {
  /* Стили для мобильных */
}

/* Medium devices (tablets, 640px - 1023px) */
@media (min-width: 640px) and (max-width: 1023px) {
  /* Стили для планшетов */
}

/* Large devices (desktops, >= 1024px) */
@media (min-width: 1024px) {
  /* Стили для десктопов */
}
```

### Grid системы

**Для секции проектов:**
```css
.projects-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 32px;
}

/* Мобильная версия */
@media (max-width: 768px) {
  .projects-grid {
    grid-template-columns: 1fr;
    gap: 24px;
  }
}
```

**Для навыков:**
```css
.skills-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 24px;
}
```

### Flexbox для Hero section
```css
.hero-content {
  display: flex;
  flex-direction: column;
  align-items: center;
  text-align: center;
  min-height: 100vh;
  justify-content: center;
}

@media (min-width: 768px) {
  .hero-content {
    flex-direction: row;
    text-align: left;
  }
}
```

---

## ✨ Анимации и интерактивность

### Базовые transitions
```css
/* Плавные переходы для всех интерактивных элементов */
a, button, .card {
  transition: all 0.3s ease;
}

/* Hover эффекты */
a:hover {
  opacity: 0.8;
}

.card:hover {
  transform: translateY(-5px);
  box-shadow: 0 20px 40px rgba(0,0,0,0.1);
}
```

### Анимации при скролле (с AOS)
```html
<!-- В HTML -->
<div data-aos="fade-up" data-aos-duration="800">
  Контент
</div>
```

```javascript
// В JS
AOS.init({
  duration: 800,
  once: true,
  offset: 100
});
```

**Варианты анимаций:**
- `fade-up` - появление снизу
- `fade-in` - простое появление
- `zoom-in` - увеличение
- `slide-left/right` - слайд

### Кастомные анимации
```css
@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-20px);
  }
}

.floating-element {
  animation: float 3s ease-in-out infinite;
}

/* Анимация появления текста */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-title {
  animation: fadeInUp 1s ease-out;
}
```

### Интерактивность

#### Smooth scroll к якорям
```javascript
// Плавный скролл к секциям
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function(e) {
    e.preventDefault();
    const target = document.querySelector(this.getAttribute('href'));
    
    if (target) {
      target.scrollIntoView({
        behavior: 'smooth',
        block: 'start'
      });
    }
  });
});
```

#### Активная навигация
```javascript
// Подсветка активной секции в меню
const sections = document.querySelectorAll('section');
const navLinks = document.querySelectorAll('.nav-link');

window.addEventListener('scroll', () => {
  let current = '';
  
  sections.forEach(section => {
    const sectionTop = section.offsetTop;
    const sectionHeight = section.clientHeight;
    
    if (scrollY >= (sectionTop - 200)) {
      current = section.getAttribute('id');
    }
  });
  
  navLinks.forEach(link => {
    link.classList.remove('active');
    if (link.getAttribute('href').includes(current)) {
      link.classList.add('active');
    }
  });
});
```

#### Мобильное меню (гамбургер)
```javascript
const burger = document.querySelector('.burger');
const nav = document.querySelector('.nav-links');

burger.addEventListener('click', () => {
  nav.classList.toggle('active');
  burger.classList.toggle('active');
});

// Закрытие меню при клике на ссылку
document.querySelectorAll('.nav-link').forEach(link => {
  link.addEventListener('click', () => {
    nav.classList.remove('active');
    burger.classList.remove('active');
  });
});
```

---

## ⚡ Оптимизация и производительность

### Оптимизация изображений
1. Используй WebP формат с fallback
```html
<picture>
  <source srcset="image.webp" type="image/webp">
  <img src="image.jpg" alt="Description">
</picture>
```

2. Ленивая загрузка
```html
<img src="image.jpg" loading="lazy" alt="Description">
```

3. Оптимальные размеры:
- Проекты: 800x600px
- Профиль: 400x400px
- Иконки: SVG

### Минификация
```bash
# Можно использовать онлайн инструменты:
# - CSS: cssnano, clean-css
# - JS: terser, uglify-js
# - HTML: html-minifier
```

### Критический CSS
```html
<head>
  <!-- Inline critical CSS -->
  <style>
    /* Стили для above-the-fold контента */
    body { margin: 0; font-family: 'Inter', sans-serif; }
    .hero { min-height: 100vh; }
  </style>
  
  <!-- Остальные стили грузим асинхронно -->
  <link rel="preload" href="css/style.css" as="style" onload="this.onload=null;this.rel='stylesheet'">
</head>
```

### Производительность
```html
<!-- Preconnect для внешних ресурсов -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="dns-prefetch" href="https://fonts.googleapis.com">

<!-- Defer для JS -->
<script src="js/main.js" defer></script>
```

---

## 🚀 Деплой и SEO

### GitHub Pages (бесплатно)
1. Создай репозиторий `username.github.io`
2. Залей файлы
3. Включи GitHub Pages в настройках
4. Сайт доступен по адресу: `https://username.github.io`

### Netlify (рекомендую)
1. Зарегистрируйся на netlify.com
2. Подключи GitHub репозиторий
3. Деплой автоматический при каждом push
4. Бесплатный SSL и кастомный домен

### SEO оптимизация

#### Meta теги
```html
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <!-- SEO -->
  <title>Владислав Пашечко - Fullstack разработчик</title>
  <meta name="description" content="Fullstack-разработчик с опытом в Vue.js, React, Python. Создаю современные веб-приложения и Telegram-ботов.">
  <meta name="keywords" content="fullstack разработчик, веб-разработчик, vue.js, react, python">
  <meta name="author" content="Владислав Пашечко">
  
  <!-- Open Graph (для соцсетей) -->
  <meta property="og:title" content="Владислав Пашечко - Fullstack разработчик">
  <meta property="og:description" content="Fullstack-разработчик с опытом в Vue.js, React, Python">
  <meta property="og:image" content="https://yoursite.com/preview.jpg">
  <meta property="og:url" content="https://yoursite.com">
  <meta property="og:type" content="website">
  
  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Владислав Пашечко - Fullstack разработчик">
  <meta name="twitter:description" content="Fullstack-разработчик с опытом в Vue.js, React, Python">
  
  <!-- Favicon -->
  <link rel="icon" type="image/x-icon" href="assets/favicon/favicon.ico">
</head>
```

#### Семантический HTML
```html
<!-- Используй правильные теги -->
<header>
  <nav>
    <ul>
      <li><a href="#about">О себе</a></li>
    </ul>
  </nav>
</header>

<main>
  <section id="hero">
    <h1>Заголовок</h1>
  </section>
</main>

<footer>
  <p>&copy; 2024</p>
</footer>
```

---

## ✅ Чеклист перед публикацией

### Функциональность
- [ ] Все ссылки работают
- [ ] Резюме доступно для скачивания
- [ ] Форма обратной связи работает (если есть)
- [ ] Навигация плавно скроллит к секциям
- [ ] Мобильное меню работает корректно

### Дизайн и UX
- [ ] Сайт выглядит хорошо на всех устройствах (320px - 1920px)
- [ ] Все изображения загружаются
- [ ] Нет горизонтального скролла
- [ ] Читаемая типографика (размеры, контраст)
- [ ] Hover эффекты работают

### Производительность
- [ ] Изображения оптимизированы (WebP, сжатие)
- [ ] CSS и JS минифицированы
- [ ] Нет неиспользуемого кода
- [ ] Lazy loading для изображений
- [ ] Скорость загрузки < 3 секунд

### SEO и доступность
- [ ] Meta теги заполнены
- [ ] Заголовки H1-H6 правильно используются
- [ ] Alt теги у всех изображений
- [ ] Семантический HTML
- [ ] Фавикон установлен

### Кросс-браузерность
- [ ] Работает в Chrome
- [ ] Работает в Firefox
- [ ] Работает в Safari
- [ ] Работает в Edge

### Тестирование
Проверь сайт через:
- Google PageSpeed Insights
- GTmetrix
- Wave (accessibility)
- W3C Validator

---

## 🎯 Дополнительные фичи (опционально)

### Тёмная тема
```javascript
// Toggle для переключения темы
const themeToggle = document.querySelector('.theme-toggle');
const html = document.documentElement;

themeToggle.addEventListener('click', () => {
  html.classList.toggle('dark-theme');
  localStorage.setItem('theme', 
    html.classList.contains('dark-theme') ? 'dark' : 'light'
  );
});

// Загрузка сохранённой темы
const savedTheme = localStorage.getItem('theme');
if (savedTheme === 'dark') {
  html.classList.add('dark-theme');
}
```

### Частицы на фоне (particles.js)
```html
<script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
<script>
  particlesJS('particles-js', {
    particles: {
      number: { value: 80 },
      color: { value: "#3B82F6" },
      shape: { type: "circle" },
      opacity: { value: 0.5 },
      size: { value: 3 },
      line_linked: {
        enable: true,
        distance: 150,
        color: "#3B82F6",
        opacity: 0.4
      },
      move: { enable: true, speed: 2 }
    }
  });
</script>
```

### Типирующийся текст (typed.js)
```html
<span id="typed"></span>

<script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.12"></script>
<script>
  new Typed('#typed', {
    strings: ['Fullstack разработчик', 'Vue.js энтузиаст', 'Python программист'],
    typeSpeed: 50,
    backSpeed: 30,
    loop: true
  });
</script>
```

---

## 📚 Полезные ресурсы

### Вдохновение для дизайна:
- [Dribbble - Portfolio](https://dribbble.com/search/portfolio)
- [Awwwards](https://www.awwwards.com/)
- [Behance](https://www.behance.net/)

### Цветовые палитры:
- [Coolors](https://coolors.co/)
- [Color Hunt](https://colorhunt.co/)
- [Tailwind Colors](https://tailwindcss.com/docs/customizing-colors)

### Инструменты:
- [Squoosh](https://squoosh.app/) - сжатие изображений
- [Canva](https://www.canva.com/) - создание превью проектов
- [Figma](https://www.figma.com/) - дизайн макета

---

## 🚀 План действий (Step-by-step)

### День 1: Структура и контент
1. Создай структуру файлов
2. Напиши HTML разметку для всех секций
3. Подготовь контент (тексты, описания)
4. Оптимизируй и залей изображения

### День 2: Стилизация
1. Настрой CSS переменные (цвета, шрифты)
2. Создай базовые стили (типографика, кнопки, карточки)
3. Стилизуй секции по порядку
4. Добавь адаптивность (mobile-first)

### День 3: Интерактивность
1. Подключи библиотеки (AOS, Font Awesome)
2. Напиши JS для навигации и меню
3. Добавь анимации
4. Протестируй на всех устройствах

### День 4: Полировка и деплой
1. Оптимизируй производительность
2. Проверь всё по чеклисту
3. Добавь SEO meta теги
4. Задеплой на Netlify/GitHub Pages
5. Протестируй финальную версию

---

## 💡 Советы от опытных разработчиков

1. **Не перегружай анимациями** - лучше меньше, да лучше
2. **Mobile-first подход** - начинай дизайн с мобильной версии
3. **Показывай результат, а не процесс** - фокус на проектах, а не на "я учусь"
4. **Простота > сложность** - чистый минималистичный дизайн выигрывает
5. **Быстрая загрузка важнее красоты** - оптимизируй всё
6. **Реальные проекты важнее пет-проектов** - покажи коммерческий опыт
7. **Доступность = профессионализм** - тестируй на скринридерах

---

## 🎉 Заключение

Следуя этому роадмапу, ты создашь профессиональный сайт-визитку за 3-4 дня. 

**Главное помни:**
- Сайт должен быть **быстрым**
- Дизайн должен быть **чистым**
- Контент должен быть **релевантным**
- Код должен быть **качественным**

Удачи в создании! 🚀

---

**P.S.** Если нужна помощь на любом этапе - обращайся, помогу с конкретными задачами!
